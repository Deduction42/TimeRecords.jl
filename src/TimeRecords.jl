module TimeRecords
    include(joinpath(@__DIR__, "__assembly.jl"))
    export 
        AbstractTimeRecord, 
        AbstractTimeSeries, 
        TimeRecord, 
        TimeInterval, 
        TimeSeries, 
        interpolate,
        strictinterp,
        average,
        integrate,
        accumulate,
        timestamp,
        timestamps,
        datetime,
        datetimes,
        value,
        values,
        dropnan!,
        dropnan,
        mapvalues,
        mapvalues!,
        getinner,
        getouter,
        viewinner,
        viewouter,
        findinner,
        findouter,
        foundbounds
end
